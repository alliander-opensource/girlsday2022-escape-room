.PHONY=clean

SOURCE_DIR=src
SOURCE_NAME=switch
TARGET_DIR=target
ARTIFACT_NAME=switch

SOURCE=${SOURCE_DIR}/${SOURCE_NAME}.ps
ARTIFACT=${TARGET_DIR}/${ARTIFACT_NAME}.pdf

${ARTIFACT}: ${SOURCE} ${TARGET_DIR}
	ps2pdf $< $@

${TARGET_DIR}:
	mkdir -p $@


clean:
	rm -r ${ARTIFACT}